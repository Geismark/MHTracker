scores -> get main to export to CSV
need to find a way to properly format strings to put into csv (formatData())

'Master window'
select output/read filepath

Help buttons
plotlib

data validation
pyTest - unitTests
dataclass (layouts) // static vars **************
""" DOC string """

https://www.w3schools.com/python/python_lists_comprehension.asp

formatting
set env var for logging level in logging (use os environment)
	LOGGING_LEVEL="INFO" python main.py

Helvitica 12 bold underline

add questions to file and add them to layouts
- add to .gitignore

default sliders and checkboxes to blank?
- BUT no way to determine if neglected or left at default? Need different input style? ---Or all default 0? [NO]---


ADD: implemented new data manipulation, static testing, auto-close on submit, logging, better imports, fixed function in time rather than manipulate module
ADD: hid questions (still functional with blank questions), added seperate content package within lib, refactored getRadioText, outputMod to be worked on next
ADD: export questionnaire to CSV, base main window
ADD: Graph plotting in main window